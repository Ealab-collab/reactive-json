/**
 * Mock for static assets (images, fonts, videos, etc.)
 * Used by Jest to handle imports of static files in tests.
 * Prevents test failures when code imports assets like:
 * - import logo from './logo.png'
 * - import font from './font.woff2'
 */
export default 'test-file-stub';

